`use client`;
import React from 'react'

export const ProductCard = () => {
  return (
    <div>
      <h1>productCard</h1>
      <button onClick={()=> console.log("add")}>add</button>
    </div>
  )
}
